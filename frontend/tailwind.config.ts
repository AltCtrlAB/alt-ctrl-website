/**
 * tailwind.config.ts — Tailwind CSS v4 stub
 *
 * ⚠️  Tailwind CSS v4 is CSS-first: theme configuration lives entirely in
 *     the `@theme inline { … }` block inside `src/index.css`, not here.
 *
 * This file exists as a placeholder so the project structure matches
 * expectations. The @tailwindcss/vite plugin picks up configuration
 * automatically from the CSS file — no JS config needed.
 *
 * See: https://tailwindcss.com/docs/v4-beta#css-first-configuration
 */

export default {}
